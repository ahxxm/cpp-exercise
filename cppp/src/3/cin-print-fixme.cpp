#include <iostream>
#include <string>
#include <cctype>


int main(void) {

  const std::string hexdigits("0123456789ABCDEF");
  std::string result;
  std::string::size_type n = 0;

  // while (std::cin >> n) {
  // if (n < hexdigits.size()) {
  // result += hexdigits[n];
  // }
  // }

  std::cout << result << std::endl;
  std::cout << n << std::endl;

}
